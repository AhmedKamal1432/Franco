variables name one char
int , float 4byte
char 1 byte
no assign at declaration
we don't have boolens

jz jump if the zero flag raised 

compare between two values in the stack  if the condition FALSE the zero flag RAISED
and pop them form the stack

and pop the two values in the stack

logical operator take from stack and push result to it

push pop not change in the flags

no concating assignments k = p = i 

